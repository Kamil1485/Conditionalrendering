import React from 'react';

const Form = () => {
    return (
        <div>
            <select>
                <option>1</option>
            </select>
        </div>
    );
}

export default Form;
